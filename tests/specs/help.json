[
  {
    "name": "/help explains monitoring",
    "steps": [
      { "send": { "text": "/help" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Use /start to configure your channel and trade percentage.\n\nAdd this bot as a channel admin so it can receive signal posts. Valid signals need an asset and CALL or PUT direction." } }] }
    ]
  },
  {
    "name": "Help menu button shows help",
    "steps": [
      { "send": { "callback": "menu:help" }, "expect": [{ "method": "editMessageText" }] }
    ]
  }
]
