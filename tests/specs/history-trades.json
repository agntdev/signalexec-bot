[
  {
    "name": "View empty trade history",
    "steps": [
      { "send": { "callback": "history:trades" }, "expect": [{ "method": "sendMessage", "payload": { "text": "No trades yet — valid channel signals will appear here." } }] }
    ]
  }
]
