[
  {
    "name": "Configure Channel and save it",
    "steps": [
      { "send": { "callback": "config:channel" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Send the channel ID or @username to monitor." } }] },
      { "send": { "text": "-1001234567890" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Channel updated. I’ll send valid signals here." } }] }
    ]
  }
]
